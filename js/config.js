/* =========================================================
   Gimna — configuração de ligação ao backend
   Troca este URL pelo do teu serviço no Render assim que o
   tiveres (ex.: "https://animakids-backend.onrender.com").
   Deixa como null para a app funcionar só em modo offline-local
   (o comportamento que já tinhas antes desta ligação).
   ========================================================= */
window.API_BASE_URL = "http://localhost:3001";
